import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapperCheckBox: {
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: -15,
  },
});
export default styles;
